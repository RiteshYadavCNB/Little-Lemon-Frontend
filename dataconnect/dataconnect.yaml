specVersion: "v1beta"
serviceId: "little-lemon"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "little-lemon-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
